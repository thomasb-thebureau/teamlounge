name: CI
on:
  push:
    branches: [main]
    paths-ignore: [data.json, updated_at.txt]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 -c "import json; json.load(open('data.json'))"
      - run: grep -q '</html>' index.html
